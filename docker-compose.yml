version: "3.8"

services:
  dailymood:
    build: .
    container_name: dailymood
    restart: unless-stopped
    ports:
      - "8501:8501"
    volumes:
      - ./entries:/app/entries
      - ./data:/app/data
